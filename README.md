# KB Physical Wellness Website

Simple static website for KB Physical Wellness.

## Running Locally

To serve the site locally, use:

```bash
npx http-server -p 8080 -o
```

This will:
- Start a local server on port 8080
- Automatically open the site in your browser
- No installation required (uses npx)

To stop the server, press `CTRL-C`
